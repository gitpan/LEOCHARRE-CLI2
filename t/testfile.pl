#!/usr/bin/perl
use strict;
use lib './lib';
use LEOCHARRE::CLI2 'ARGV_PATHS';
our $VERSION = sprintf "%d.%02d", q$Revision: 1.1 $ =~ /(\d+)/g;

debug('debug is on');

debug(" files count: $ARGV_FILES_COUNT, dirs count: $ARGV_DIRS_COUNT");




exit;










